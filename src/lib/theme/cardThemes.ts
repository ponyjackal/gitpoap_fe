import { BackgroundPanel } from '../../colors';

export const cardThemes = {
  styles: () => ({
    defaultProps: {
      radius: 6,
    },
    root: {
      background: BackgroundPanel,
    },
  }),
};
